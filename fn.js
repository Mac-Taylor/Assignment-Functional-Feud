// Make this one the module for Functional Feud assignment

module.exports = {
    range: range,
    _map: _map,
    _filter: _filter,
    
}

function range(min, max) {
    // Input: two numbers, Return Value: Array of Numbers
    // High-level = take an input 

};

function _map(array, func) {

};

function _filter(array, func) {

};



